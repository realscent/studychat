{
  "name": "button-push-site",
  "version": "1.0.0",
  "description": "Classroom realtime chat and completion button tracker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check src/state.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5",
    "socket.io-parser": "^4.2.7"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
